{{- if and .Values.prometheus.enabled (not .Release.IsInstall) -}}
apiVersion: monitoring.coreos.com/v1
kind: Alertmanager
metadata:
  name: main
  labels:
    alertmanager: main
spec:
  replicas: 3
  version: v0.7.1
{{- end }}